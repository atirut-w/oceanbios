#pragma once

#define SERIALTERM 0x2ff

void putchar(const char c); // Print a character to the terminal.
void print(const char *s); // Print a string to the terminal.
void printbyte(const char b); // Print a byte to the terminal.
void printshort(const short s); // Print a short to the terminal.
